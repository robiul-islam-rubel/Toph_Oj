#include <iostream>

using namespace std;

int main() {
	int N;
	cin >> N;
	int k=1;
	for(int i=2;i<=N;i++)
		{
			k=k*i;
			k=k%10000;
		}
	if(k==0)
	{
		cout<<"0000"<<endl;
	}
	else
	{
		cout<<k<<endl;
	}
	
	return 0;
}
